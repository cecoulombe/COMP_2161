<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="50dp"
    android:gravity="center"
    android:background="@color/appBackground"
    tools:context=".MainActivity">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/PasscodeTitle"
        android:text="@string/PasscodeTitle" />

    <!-- Passcode -->
    <TextView
        android:id="@+id/passcodeTextview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/PasscodeTextview"
        android:elevation="8dp"
        android:text="@string/DefaultPass" />

    <!-- Countdown -->
    <TextView
        android:id="@+id/countdownTextview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/CountdownTextview"
        android:text="@string/DefaultCountdown" />

    <!-- Countdown -->
    <Button
        android:id="@+id/verifyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:backgroundTint="@null"
        android:text="@string/VerifyButton"
        style="@style/VerifyButton"/>
</LinearLayout>